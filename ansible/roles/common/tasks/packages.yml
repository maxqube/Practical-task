- name: Do an yum update & upgrade
  become: yes
  yum:
    name: '*'
    state: latest

- name: Install required packages
  become: yes
  package:
    name: "{{ item }}"
    state: present
  with_items:
    - sudo